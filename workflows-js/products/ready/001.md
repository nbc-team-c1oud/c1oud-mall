
- MockCartService 교체
- MockPointService 교체


- Product 팀 카테고리, 정렬 페이지 요청 
- Cart 영역 확인
    retry만 뜨고 결제 추가 진행 안됨
- 웹훅 관련해서 확인할 방법 


- point 연동 서비스 - pay 관련 확인 

- 시나리오 재고 

-orderItemId 수정 시나리오 
동작 정책
    - 결제 완료 후 orderStatus=PENDING_PAYMENT라도 배지는 "결제 완료"로 표시 + 하단 hint "주문 확정 처리에 지연이 있을 수 있어요"
    - 헤더 메타에 BE 원본 두 상태(주문 상태/결제 상태) 그대로 노출 → 디버깅 도움
    - 환불 응답 분기는 refundStatus 값으로 판별 (PG_CANCELLED = 완료 / DB_COMMITTED = 처리 중)

사용자에게 보고할 사항 (BE 측 필요)
│ src/pages/OrderDetailPage.tsx         │ 배지 통합 + 진단 Row(주문상태/결제상태) + hint + 환불 버튼/대화상자 통합 │
└───────────────────────────────────────┴──────────────────────────────────────────────────────────────────────────┘

동작 정책
    - 결제 완료 후 orderStatus=PENDING_PAYMENT라도 배지는 "결제 완료"로 표시 + 하단 hint "주문 확정 처리에 지연이 있을 수 있어요"
    - 헤더 메타에 BE 원본 두 상태(주문 상태/결제 상태) 그대로 노출 → 디버깅 도움
    - 환불 응답 분기는 refundStatus 값으로 판별 (PG_CANCELLED = 완료 / DB_COMMITTED = 처리 중)

사용자에게 보고할 사항 (BE 측 필요)
    1. OrderItemResponse에 orderItemId 필드 추가 (한 줄) — 현재 BE 응답에 없어서 환불 버튼이 비활성 + 안내로 fallback 상태입니다. BE OrderItemResponse.from(orderItem)에서
       orderItem.getId()를 orderItemId로 매핑해주시면 환불 다이얼로그가 바로 활성화됩니다.
    2. paymentStatus=COMPLETED ∧ orderStatus=PENDING_PAYMENT 불일치 원인 — BE PaymentConfirmationService / Order.markAsConfirmed 분기 점검 필요. FE는 통합 라벨링으로 사용자 혼란만   
       차단해둔 상태.

dev 서버는 이미 떠있으니 브라우저에서 /orders/{id} 새로고침해서 표시 변화 확인 가능합니다.

